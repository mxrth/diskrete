Source in components.C

Testscript laufen lassen:
    make #evtl make CXX=g++ falls clang nicht installiert ist
    test/test.sh #muss aus dem Ordner über Test aufgerufen werden

Programm auf Graph ausführen:
    make
    bin/components /path/to/graph
